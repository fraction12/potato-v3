# Tasks — phase-8b-project

- [ ] T-811: Per-project state persistence — .potato/state.db stores roles, messages, shared context, tasks across sessions
- [ ] T-812: Project-scoped agent roster — .potato/agents.toml defines per-project agents with models, args, env overrides
